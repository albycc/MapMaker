export interface ILegend {
  id: string;
  label: string;
  colour: string;
}

export interface ILegendStyles {
  borderColor: string;
  borderWidth: number;
  borderRound: number;
  backgroundColor: string;
  fontColor: string;
  titleSize: number;
  spaceBetweenRows: number;
}
